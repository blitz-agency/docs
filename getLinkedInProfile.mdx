# API d'Enrichissement LinkedIn - BlitzAPI

## Vue d'ensemble

L'API d'enrichissement LinkedIn de BlitzAPI transforme instantanément des URLs LinkedIn en données structurées complètes. Conçue pour la simplicité et la performance, elle vous donne accès aux informations publiques des profils et entreprises LinkedIn.

## Caractéristiques techniques

### 🚀 Performance
- **Latence ultra-faible** : Réponses en temps réel
- **Infrastructure scalable** : Gestion de volumes importants
- **Pass-through natif** : Données brutes sans transformation

### 💰 Système de crédits
- **Profil personnel** : 1 crédit par enrichissement
- **Page entreprise** : 1 crédit par enrichissement
- **Facturation à l'usage** : Payez uniquement ce que vous consommez
- **Sans engagement** : Flexibilité totale

### 🔒 Fiabilité
- **Validation stricte** : Contrôle des URLs avant traitement
- **Gestion d'erreurs robuste** : Codes HTTP standards

## Processus d'enrichissement

### 1. Authentification
Utilisez votre clé API unique dans le header `Authorization`:
```
Authorization: Bearer YOUR_API_KEY
```

### 2. Appel API
Envoyez l'URL LinkedIn à enrichir :
```json
{
  "url": "https://www.linkedin.com/in/john-doe"
}
```

### 3. Réponse structurée
Recevez les données complètes au format JSON, organisées par sections.

## Endpoints

### 📤 `POST /enrich/linkedin/person`
**Coût : 1 crédit**

Enrichit un profil personnel LinkedIn avec :
- **Identité** : nom, prénom, headline, photo, localisation
- **Expérience** : `positionGroups` avec historique complet
- **Formation** : `educations` avec établissements et diplômes
- **Certifications** : `certifications` avec autorités et dates
- **Compétences** : `skills` listées
- **Langues** : `languages` avec niveaux
- **Bénévolat** : `volunteerExperiences`
- **Structure paginée** : `totalElements`, `page`, `size`, `contents`

### 🏢 `POST /enrich/linkedin/company`
**Coût : 1 crédit**

Enrichit une page entreprise LinkedIn avec :
- **Identité** : name, logo, cover, tagline
- **Données** : type, foundedYear, objectUrn
- **Effectifs** : `staff` avec total et range
- **Localisations** : `headquarter` et `other` bureaux
- **Activité** : industries, website, followers
- **Metadata** : hashtags, specialities

## Gestion des URLs

### Validation stricte

#### ✅ URLs acceptées
**Personnes :**
- `https://www.linkedin.com/in/identifier`
- `https://linkedin.com/in/identifier/`
- `https://www.linkedin.com/pub/name/1/2/3`

**Entreprises :**
- `https://www.linkedin.com/company/identifier`
- `https://linkedin.com/company/identifier/`

#### ❌ URLs refusées
- Sous-domaines : `fr.linkedin.com`, `uk.linkedin.com`
- Pages école : `/school/`
- Posts/Articles : `/pulse/`, `/feed/`
- Recherches : `/search/`
- Groupes : `/groups/`

### Normalisation automatique
- Ajout HTTPS si manquant
- Suppression des paramètres UTM
- Nettoyage des espaces et slash finaux

## Format de réponse

### Structure paginée (Person)
```json
{
  "firstname": "John",
  "lastname": "Doe",
  "educations": {
    "totalElements": 2,
    "page": 0,
    "size": 20,
    "contents": [...]
  }
}
```

### Structure directe (Company)
```json
{
  "name": "Company Name",
  "staff": {
    "total": 500,
    "range": {
      "start": 201,
      "end": 500
    }
  }
}
```

## Codes d'erreur

| Code | Status | Description | Action |
|------|--------|-------------|--------|
| 401 | UNAUTHORIZED | Clé API invalide | Vérifier la clé |
| 422 | UNPROCESSABLE_ENTITY | URL invalide ou manquante | Corriger l'URL |
| 500 | INTERNAL_SERVER_ERROR | Erreur serveur | Contacter support |

### Format d'erreur
```json
{
  "error": "UNAUTHORIZED",
  "message": "Invalid API key provided"
}
```

## Bonnes pratiques

### ✅ Recommandations
- **Valider les URLs** côté client avant envoi
- **Gérer les erreurs** avec retry exponential backoff
- **Logger les appels** pour audit et debug
- **Respecter la privacy** des données collectées

### ❌ À éviter
- URLs avec sous-domaines pays
- Requêtes parallèles massives sans contrôle
- Ignorer les codes d'erreur 4xx

---

*BlitzAPI - Enrichissement LinkedIn simple, rapide et fiable*

*Dernière mise à jour : Septembre 2025*
